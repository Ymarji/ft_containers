// #include <vector>
#include "./vector.hpp"
#include <iostream>
#include <iterator>
int main(){
    ft::vector<int> v(5, 200);
    // std::vector<int> v(100);

    // std::allocator<int> Alloc;

    // int *arr =  Alloc.allocate(0);
    // v.push_back(24);
    // // v.push_back(24);
    // // v.push_back(24);
    // // v.push_back(24);
    // // v.push_back(24);
    // // v.push_back(24);
    // // v.push_back(24);
    // // v.push_back(24);
    // // v.push_back(24);

    // std::cout << v.capacity() << "  " << v.size();
    return 0;
}